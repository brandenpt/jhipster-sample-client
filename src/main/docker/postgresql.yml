version: '2'
services:
  brandenportal-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/BrandenPortal/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=BrandenPortal
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
